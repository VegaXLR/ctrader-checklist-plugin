# Spotware SDK

SDK for messaging using External Api with host

## Installation

```bash
# If you use npm:
npm install @spotware-web-team/sdk

# Or if you use Yarn:
yarn add @spotware-web-team/sdk
```

## Documentation

### All methods using RxJs

### Create Client Api

```typescript
import {createClientAdapter} from '@spotware-web-team/sdk-external-api';

const clientApi = createClientAdapter();
```

### Use it to communicate with the host

```typescript
import {confirmEvent, registerEvent} from '@spotware/sdk';

confirmEvent(clientApi).pipe(mergeMap(() => ([])));
registerEvent(clientApi).pipe(mergeMap(() => ([])));
```

### Use trading function

```typescript
import {getAccountInformation} from '@spotware/sdk';

getAccountInformation(clientApi).pipe(mergeMap(() => ([])));
```

## License

[MIT](LICENSE.md)
